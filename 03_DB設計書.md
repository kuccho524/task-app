# DB設計書
## 1. 概要
Prisma schemaの完全版は `prisma/schema.prisma` を参照。
本設計書では、命名規則と主要リレーションのみ記載する。

## 2. DB設計
### 2.1 テーブル一覧
| テーブル名 | 内容 |
|---|---|
| users | ユーザー情報 |
| projects | プロジェクト情報 |
| tasks | タスク情報 |
| priorities | 優先度マスタ |
| statuses | ステータスマスタ |
| comments | コメント情報 |

### users
| カラム名 | 型 | 必須 | 主キー | 外部キー | 備考 |
|---|---|---|---|---|---|
| user_id | uuid | ○ | ○ |  | ユーザーID |
| user_name | varchar(100) | ○ |  |  | ユーザー名 |
| user_email | varchar(255) | ○ |  |  | ログインID。一意制約 |
| password_hash | varchar(255) | ○ |  |  | ハッシュ化済みパスワード |
| created_at | timestamp | ○ |  |  | 作成日時 |
| updated_at | timestamp | ○ |  |  | 更新日時 |

- user_email は一意とする
- password_hash には平文パスワードを保存しない

### projects
| カラム名 | 型 | 必須 | 主キー | 外部キー | 備考 |
|---|---|---|---|---|---|
| project_id | uuid | ○ | ○ |  | プロジェクトID |
| project_name | varchar(100) | ○ |  |  | プロジェクト名 |
| assignee_id | uuid | ○ |  | users.user_id | プロジェクト担当者 |
| priority_id | uuid | ○ |  | priorities.priority_id | 優先度 |
| status_id | uuid | ○ |  | statuses.status_id | ステータス |
| description | text |  |  |  | プロジェクト説明 |
| start_date | timestamp / DateTime |  |  |  | 開始予定日 |
| deadline | timestamp / DateTime |  |  |  | 期限 |
| completed_at | timestamp / DateTime |  |  |  | 完了日時 |
| created_by | uuid | ○ |  | users.user_id | 作成者 |
| created_at | timestamp | ○ |  |  | 作成日時 |
| updated_at | timestamp | ○ |  |  | 更新日時 |

- deadline は start_date 以降の日付とする
- status.is_completed = true のステータスになった場合、completed_at に現在日時を設定する
- status.is_completed = false に戻した場合、completed_at は null に戻す

### tasks
| カラム名 | 型 | 必須 | 主キー | 外部キー | 備考 |
|---|---|---|---|---|---|
| task_id | uuid | ○ | ○ |  | タスクID |
| project_id | uuid | ○ |  | projects.project_id | 紐づくプロジェクト |
| task_name | varchar(100) | ○ |  |  | タスク名 |
| assignee_id | uuid | ○ |  | users.user_id | タスク担当者 |
| priority_id | uuid | ○ |  | priorities.priority_id | 優先度 |
| status_id | uuid | ○ |  | statuses.status_id | ステータス |
| description | text |  |  |  | タスク説明 |
| start_date | timestamp / DateTime |  |  |  | 開始予定日 |
| deadline | timestamp / DateTime |  |  |  | 期限 |
| completed_at | timestamp / DateTime |  |  |  | 完了日時 |
| created_by | uuid | ○ |  | users.user_id | 作成者 |
| created_at | timestamp | ○ |  |  | 作成日時 |
| updated_at | timestamp | ○ |  |  | 更新日時 |

- task は必ず project に紐づく
- deadline は start_date 以降の日付とする
- 完了ステータスになった場合、completed_at を設定する

### priorities
| カラム名 | 型 | 必須 | 主キー | 外部キー | 備考 |
|---|---|---|---|---|---|
| priority_id | uuid | ○ | ○ |  | 優先度ID |
| priority_name | varchar(50) | ○ |  |  | 優先度名 |
| sort_order | integer | ○ |  |  | 表示順 |
| created_at | timestamp | ○ |  |  | 作成日時 |
| updated_at | timestamp | ○ |  |  | 更新日時 |

初期データ:
| priority_name | sort_order |
|---|---:|
| 高 | 1 |
| 中 | 2 |
| 低 | 3 |

### statuses
| カラム名 | 型 | 必須 | 主キー | 外部キー | 備考 |
|---|---|---|---|---|---|
| status_id | uuid | ○ | ○ |  | ステータスID |
| status_name | varchar(50) | ○ |  |  | ステータス名 |
| sort_order | integer | ○ |  |  | 表示順 |
| is_completed | boolean | ○ |  |  | 完了扱いかどうか |
| created_at | timestamp | ○ |  |  | 作成日時 |
| updated_at | timestamp | ○ |  |  | 更新日時 |

初期データ:
| status_name | sort_order | is_completed |
|---|---:|---|
| 未着手 | 1 | false |
| 対応中 | 2 | false |
| 確認中 | 3 | false |
| 完了 | 4 | true |
| 保留 | 5 | false |

### comments
| カラム名 | 型 | 必須 | 主キー | 外部キー | 備考 |
|---|---|---|---|---|---|
| comment_id | uuid | ○ | ○ |  | コメントID |
| user_id | uuid | ○ |  | users.user_id | コメント投稿者 |
| content | text | ○ |  |  | コメント本文 |
| project_id | uuid | ○ |  | projects.project_id | 紐づくプロジェクト |
| task_id | uuid |  |  | tasks.task_id | 紐づくタスク |
| created_at | timestamp | ○ |  |  | 作成日時 |
| updated_at | timestamp | ○ |  |  | 更新日時 |

- プロジェクトコメントの場合：project_id あり、task_id は null
- タスクコメントの場合：project_id あり、task_id あり
- MVPではタスクコメントを優先実装する

## 3. リレーション
users 1 ─── * projects.created_by
users 1 ─── * projects.assignee_id
users 1 ─── * tasks.created_by
users 1 ─── * tasks.assignee_id
users 1 ─── * comments.user_id

projects 1 ─── * tasks
projects 1 ─── * comments

tasks 1 ─── * comments

priorities 1 ─── * projects
priorities 1 ─── * tasks

statuses 1 ─── * projects
statuses 1 ─── * tasks

## 4. Prisma schema案
model User {
  userId       String   @id @default(uuid()) @map("user_id")
  userName     String   @map("user_name")
  userEmail    String   @unique @map("user_email")
  passwordHash String   @map("password_hash")
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt @map("updated_at")

  assignedProjects Project[] @relation("ProjectAssignee")
  createdProjects  Project[] @relation("ProjectCreator")
  assignedTasks    Task[]    @relation("TaskAssignee")
  createdTasks     Task[]    @relation("TaskCreator")
  comments         Comment[]

  @@map("users")
}

model Project {
  projectId   String    @id @default(uuid()) @map("project_id")
  projectName String    @map("project_name")
  assigneeId  String    @map("assignee_id")
  priorityId  String    @map("priority_id")
  statusId    String    @map("status_id")
  description String?
  startDate   DateTime? @map("start_date")
  deadline    DateTime?
  completedAt DateTime? @map("completed_at")
  createdBy   String    @map("created_by")
  createdAt   DateTime  @default(now()) @map("created_at")
  updatedAt   DateTime  @updatedAt @map("updated_at")

  assignee User     @relation("ProjectAssignee", fields: [assigneeId], references: [userId])
  creator  User     @relation("ProjectCreator", fields: [createdBy], references: [userId])
  priority Priority @relation(fields: [priorityId], references: [priorityId])
  status   Status   @relation(fields: [statusId], references: [statusId])
  tasks    Task[]
  comments Comment[]

  @@map("projects")
}

model Task {
  taskId      String    @id @default(uuid()) @map("task_id")
  projectId   String    @map("project_id")
  taskName    String    @map("task_name")
  assigneeId  String    @map("assignee_id")
  priorityId  String    @map("priority_id")
  statusId    String    @map("status_id")
  description String?
  startDate   DateTime? @map("start_date")
  deadline    DateTime?
  completedAt DateTime? @map("completed_at")
  createdBy   String    @map("created_by")
  createdAt   DateTime  @default(now()) @map("created_at")
  updatedAt   DateTime  @updatedAt @map("updated_at")

  project  Project  @relation(fields: [projectId], references: [projectId])
  assignee User     @relation("TaskAssignee", fields: [assigneeId], references: [userId])
  creator  User     @relation("TaskCreator", fields: [createdBy], references: [userId])
  priority Priority @relation(fields: [priorityId], references: [priorityId])
  status   Status   @relation(fields: [statusId], references: [statusId])
  comments Comment[]

  @@map("tasks")
}

model Priority {
  priorityId   String   @id @default(uuid()) @map("priority_id")
  priorityName String   @map("priority_name")
  sortOrder    Int      @map("sort_order")
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt @map("updated_at")

  projects Project[]
  tasks    Task[]

  @@map("priorities")
}

model Status {
  statusId    String   @id @default(uuid()) @map("status_id")
  statusName  String   @map("status_name")
  sortOrder   Int      @map("sort_order")
  isCompleted Boolean  @default(false) @map("is_completed")
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")

  projects Project[]
  tasks    Task[]

  @@map("statuses")
}

model Comment {
  commentId String   @id @default(uuid()) @map("comment_id")
  userId    String   @map("user_id")
  content   String
  projectId String   @map("project_id")
  taskId    String?  @map("task_id")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  user    User    @relation(fields: [userId], references: [userId])
  project Project @relation(fields: [projectId], references: [projectId])
  task    Task?   @relation(fields: [taskId], references: [taskId])

  @@map("comments")
}

- projects、tasksにおいて、画面上は日付のみを扱い、時刻部分は利用しない。

## 5. completed_at の制御
プロジェクトまたはタスクのステータス変更時、変更後ステータスの is_completed を確認する。

- is_completed = true の場合、completed_at に現在日時を設定する
- is_completed = false の場合、completed_at を null に設定する

これにより、ステータス名に依存せず完了判定を行う。

